<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.digitalturbine.wrappertest" >
	
    <!-- 
    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="8" />
    -->
    
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".ApplicationFlow"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppBaseTheme" >

        <meta-data android:name="vodafone_inappbilling_enabled" android:value="yes"/>
        <meta-data android:name="vodafone_inappbilling" android:value="yes" />
        <meta-data android:name="vodafone-billing" android:value="true" />
        <activity
            android:name=".WrapperActivity"
            android:label="WrapperTest" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>